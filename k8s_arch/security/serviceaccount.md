# K8S ServiceAccount

tags: #objects #security

<!-- @import "[TOC]" {cmd="toc" depthFrom=1 depthTo=6 orderedList=false} -->

<!-- code_chunk_output -->

- [K8S ServiceAccount](#k8s-serviceaccount)
    - [Use ServiceAccounts](#use-serviceaccounts)
    - [ServiceAccounts Access Tokens](#serviceaccounts-access-tokens)

<!-- /code_chunk_output -->

---

generates an access token in a secret object upon creation.

### Use ServiceAccounts

**configure serviceaccount in a pod**

```yaml
apiVersion: v1
kind: Pod
metadata:
  #...
spec:
  containers:
    - name:
      #...
  serviceAccountName: jenkins-sa
```

> _automounts `ca.crt`, `namespace`, `token` files in `/var/run/secrets/kubernetes.io/serviceaccount`_
>
> - kubelet automatically rotates this token
> - token is deleted if pod is deleted

**after v1.22:** serviceAccounts no longer rely on secret tokens. tokens are generated by the **TokenRequestAPI** and are:

- audience bound
- time bound
- object bound

**before v1.24:** [**serviceAccount** [**secret** [**token**(doesn't expire)]]]
**after v1.24:** [**serviceAccount** [**token**(default expire after 1h)]]

**Documentation**:

- Service account token Secrets:
  https://kubernetes.io/docs/concepts/configuration/secret/#service-account-token-secrets
- Managing Service Accounts:
  https://kubernetes.io/docs/reference/access-authn-authz/service-accounts-admin/

disable default automount

```yaml
apiVersion: v1
kind: Pod
metadata:
  #...
spec:
  containers:
    - name:
      #...
      automountServiceAccountToken: false
```

**update serviceaccount of a resource**

```bash
kubectl set serviceaccount deploy/web-dashboard dashboard-sa
```

### ServiceAccounts Access Tokens

**create a short-lived access token** in a secret object

> serviceAccount must be created first

```bash
# expires after 1 hour
kubectl create token jenkins-sa [--duration 2h]
```

**create a long-lived access token** in a secret object

```yaml
# doesn't expire
apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: jenkins-sa
  annotations:
    kubernetes.io/service-account.name: jenkins-sa
```

see sa token

```bash
kubectl describe serviceaccount jenkins-sa | grep -i token

jq -R 'split(".") | select(length > 0) | .[0],.[1] | @base64 | fromjson' <<< <secret_token>
```

**use serviceaccount token**

```bash
curl htts://192.168.56.70:6443/api -insecure \
  --header "Authorization: Bearer <sa_access-token>"
```
